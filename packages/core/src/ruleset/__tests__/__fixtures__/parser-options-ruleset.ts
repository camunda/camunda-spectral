import { RulesetDefinition } from '@camunda8/spectral-core';

export default {
  extends: [],
  parserOptions: {
    incompatibleValues: 'off',
    duplicateKeys: 'warn',
  },
} as RulesetDefinition;
